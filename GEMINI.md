## python
- use `uv` for env an package managemnt
- use `ruff` for linting
- use `pytest` for testing, place tests in /tests

## general
- always write tests for code you write
- make sure the tests pass before handing control back to the user